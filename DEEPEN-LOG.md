# DEEPEN-LOG

- 2026-09-23 — Port nghiệp vụ **mã giảm giá / điểm voucher** từ nguồn Loyalty (`Crd_MemberVoucher` + `Crd_MemberVoucherTransaction`). Thêm entity `Voucher`/`VoucherRedemption`, `VoucherService` (quy tắc: Active + chưa hết hạn + còn điểm + còn lượt; mỗi lần trừ tối đa `PointLimit`, giảm 1 lượt, chống vượt hạn mức), seed 2 voucher mẫu, API `/api/voucher/redeem` + `api/v1/vouchers*`, 6 test. Build Release 0 error, 12/12 test pass. Commit `ef13edf` đã push origin/main.
- 2026-09-23 — Port tiếp nghiệp vụ **phát voucher từ chương trình voucher theo model xe** (nguồn `Prm_VoucherNewCar` + `Prm_VoucherNewCarDtl`/`Spec`). Gắn `Voucher` với `VoucherProgram` (`VoucherProgramId` + `ModelCode`), thêm `VoucherProgramService.IssueAsync` (dùng lại `CalcAsync` kiểm tra model/ngày giao xe/giới hạn ngày mở thẻ → tạo `Voucher` điểm = giá trị chương trình, hạn dùng = hôm nay + `ValidityPeriod`), API `/api/v1/voucher-program/issue` + action `VoucherProgram/Issue` + form phát voucher ở trang chi tiết, 3 test mới. Build Release 0 error, 22/22 test pass. Commit `5d1618e` đã push origin/main.
- 2026-09-23 — Port tiếp nghiệp vụ **thu hồi/huỷ chương trình voucher + đối soát voucher đã phát** (nguồn `Prm_VoucherNewCar_Finish` / `Prm_VoucherNewCar_Cancel`). Thêm `VoucherProgramService.FinishAsync` (chỉ từ Đã duyệt, chặn trùng ngày bắt đầu với chương trình đã hoàn tất khác, cắt ngày kết thúc chương trình đang hiệu lực trước đó về ngày bắt đầu mới − 1) và `CancelAsync` (chỉ từ Chờ duyệt/Đã duyệt), cùng `ReconciliationAsync` gom nhóm voucher đã phát theo chương trình + model (đã phát/đã dùng/chưa dùng + điểm đã phát/đã dùng/còn lại). API `/api/v1/voucher-programs/{id}/finish|cancel` + `GET /api/v1/voucher-programs/reconciliation`, action `VoucherProgram/Finish|Cancel|Reconciliation` + view đối soát, nút Hoàn tất/Huỷ ở trang chi tiết, 8 test mới. Build Release 0 error, 30/30 test pass.- 2026-09-23 — Port nghiệp vụ **chương trình khuyến mại mua xe mới** (nguồn `Prm_CarNew` + `Prm_CarNewDtl`/`Prm_CarNewSpec`, `Promotion.cs`/`Promotion.Calc.cs`). Thêm entity `CarPromotion`/`CarPromotionDtl` (mã, đại lý `DealerCode`, hiệu lực, `FlagAllModel`/`PointValAllModel`, chi tiết theo model), `CarPromotionService` với vòng đời Chờ duyệt → Đã duyệt → Hoàn tất/Đã huỷ (`ApproveAsync`/`FinishAsync`/`CancelAsync`), quy tắc mỗi đại lý chỉ 1 chương trình hiệu lực + ngày bắt đầu phải từ hôm nay và sau chương trình trước, hoàn tất cắt ngày kết thúc chương trình trước về (ngày bắt đầu mới − 1), và `CalcAsync` tính giá trị khuyến mại theo model. API `/api/v1/car-promotions*` + `/api/car-promotion/calc`, controller `CarPromotion` + view Index/Detail + mục menu, seed 1 chương trình mẫu, 15 test mới. Build Release 0 error, 45/45 test pass.
- 2026-09-23 — Port nghiệp vụ **chương trình khuyến mại chung** (nguồn `Prm_Promotion` + `Prm_PromotionMain`/`Prm_PromotionPrm` + các bảng scope `Prm_DateScope`/`Prm_DayOfWeekScope`/`Prm_TimeScope`/`Prm_MonthScope`/`Prm_DayScope`/`Prm_OrgScope`/`Prm_UserScope`/`Prm_CustomerGroupScope`). Thêm entity `PromotionProgram` (mã, `MainType`=Khuyến mại theo, `PrmType`=Hình thức KM, ngân sách, hiệu lực, cờ `FlagParallel`/`FlagMulti`/`FlagAll*`), `PromotionScope` (gom 8 loại điều kiện áp dụng về 1 bảng có phân loại), `PromotionPrm` (giảm giá sản phẩm/đơn hàng theo tiền hoặc %, có mức tối đa) và `PromotionMain` (điều kiện số lượng/tiền hàng). `PromotionProgramService` với vòng đời Chờ duyệt → Đã duyệt → Hoàn tất/Đã huỷ (`ApproveAsync`/`FinishAsync`/`CancelAsync`), `CheckScopeAsync` kiểm tra điều kiện tháng/ngày/thứ/giờ (hỗ trợ khung giờ qua nửa đêm) và `CalcAsync` tính giảm giá sản phẩm (nhân theo số lượng nếu `FlagMulti`) + giảm giá đơn hàng, chặn ở mức tối đa và không vượt giá trị đơn. API `/api/v1/promotion-programs*` + `/api/promotion/calc`, controller `PromotionProgram` + view Index/Detail + mục menu, seed 1 chương trình mẫu (giảm 10% đơn cuối tuần), 17 test mới. Build Release 0 error, 62/62 test pass. Commit `7c940fe` đã push origin/main.
- 2026-09-23 — Port nghiệp vụ **chương trình giới thiệu xe** (nguồn `Prm_CarRecommend` + `Prm_CarRecommendDtl`/`Prm_CarRecommendSpec`, `Promotion.cs`/`Promotion.Calc.cs`). Thêm entity `CarRecommend`/`CarRecommendDtl` (mã, đại lý `DealerCode`, hiệu lực, `FlagAllModel`/`PointValAllModel`, chi tiết theo model), `CarRecommendService` với vòng đời Chờ duyệt → Đã duyệt → Hoàn tất/Đã huỷ (`ApproveAsync`/`FinishAsync`/`CancelAsync`), quy tắc mỗi đại lý chỉ 1 chương trình hiệu lực + ngày bắt đầu phải từ hôm nay và sau chương trình trước, hoàn tất cắt ngày kết thúc chương trình trước về (ngày bắt đầu mới − 1), và `CalcAsync` tính giá trị thưởng giới thiệu theo model. API `/api/v1/car-recommends*` + `/api/car-recommend/calc`, controller `CarRecommend` + view Index/Detail + mục menu, seed 1 chương trình mẫu, 15 test mới. Build Release 0 error, 77/77 test pass. Commit `ad90b4f` đã push origin/main.- 2026-09-23 — Port nghiệp vụ **chương trình khuyến mại theo loại thẻ** (nguồn `Mst_PromotionProgram` + `Mst_PromotionProgramDtl`/`Mst_PromotionProgramSpec`, `Crd_DealUsePromotion`/`Crd_DealUsePromotionDtl`, `Master.Loyalty.cs`). Thêm entity `CardPromotionProgram` (mã, hiệu lực, `FlagAllDL`, trạng thái Bật/Tạm dừng), `CardPromotionProgramDtl` (hạn mức `Qty` theo `CardType` + đơn vị), `CardPromotionProgramSpec` (phạm vi đại lý) và `CardPromotionUsage` (nhật ký sử dụng theo giao dịch). `CardPromotionProgramService` với vòng đời Bật/Tạm dừng (`SetStatusAsync` chặn bật khi thiếu dòng loại thẻ hoặc thiếu đại lý khi không áp dụng tất cả), `QtyRemainAsync` (hạn mức − đã dùng), `CheckUseAsync` (chương trình đang hiệu lực + đại lý thuộc phạm vi + loại thẻ có hạn mức + còn đủ số lượng) và `UseAsync` ghi nhận sử dụng, cùng `ReconciliationAsync` đối soát hạn mức/đã dùng/còn lại theo chương trình + loại thẻ. API `/api/v1/card-promotion-programs*` + `/api/card-promotion/check|use`, controller `CardPromotionProgram` + view Index/Detail/Reconciliation + mục menu, seed 1 chương trình mẫu (GOLD/PLATINUM, đại lý DLCP01), 15 test mới. Build Release 0 error, 92/92 test pass. Commit `fa4ff3c` (push origin/main bị remote từ chối lock ref — đã commit local).
- 2026-09-23 — Port nghiệp vụ **phạm vi sản phẩm/nhóm sản phẩm áp dụng của chương trình khuyến mại chung** (nguồn `Prm_PromotionMainSpec` + `Prm_PromotionPrmSpec`, `PromotionRefType` trong `Const.Main.BE.cs`, logic lọc `#tbl_Ord_OrderDtl_Ref_Union_Filter` trong `20200311.Skycic.DMS.OrderCalc.sql`). Thêm entity `PromotionProductScope` (gom 2 bảng nguồn về 1 bảng có phân loại: `Kind`=Điều kiện/Hình thức, `RefType`=Sản phẩm/Nhóm sản phẩm/Phát voucher/Voucher, `RefCode`/`RefName`, `Idx`, `MapIdx`, `FlagActive`) + enum `PromotionRefType`/`PromotionProductScopeKind`. `PromotionProgramService.AddProductScopeAsync` (chuẩn hoá mã, chặn trùng theo Kind+RefType+RefCode) và mở rộng `CalcAsync` nhận thêm `orderLines`: khi chương trình có phạm vi sản phẩm thì chỉ tính giảm giá trên các dòng hàng khớp `RefCode`+`RefType` (không có dòng khớp → không áp dụng; không truyền dòng hàng → từ chối). API `/api/v1/promotion-programs/{id}/product-scopes` + mở rộng `/api/promotion/calc` (nhận `lines`), action `PromotionProgram/AddProductScope` + bảng phạm vi + form ở trang chi tiết, seed 2 dòng phạm vi mẫu (SP001 + nhóm GRP01), 6 test mới. Build Release 0 error, 98/98 test pass.
- 2026-09-23 — Port nghiệp vụ **chương trình tặng điểm sinh nhật** (nguồn `Mst_BirthPolicy` + `Mst_BirthPolicyDtl`, logic `Crd_Member_PerformBirhday` trong `Transaction.Birthday.cs`, tỷ lệ quy đổi `Mst_ParamSys.UNITPOINTTOMONEY`). Thêm entity `BirthdayPolicy` (mã, hiệu lực, cờ `FlagPoint`, tỷ lệ `ParamValue`, trạng thái Bật/Tạm dừng), `BirthdayPolicyDtl` (điểm tặng theo `CardType`) và `BirthdayGrant` (nhật ký tặng điểm theo hội viên/thẻ, `DealPointType='BIRTHDAY'`). `BirthdayPolicyService` với vòng đời Bật/Tạm dừng (`SetStatusAsync` chặn bật khi thiếu dòng loại thẻ), `CheckEligibilityAsync` (chương trình đang hiệu lực + có tặng điểm; ngày sinh trùng tháng-ngày với ngày xét; loại thẻ có mức điểm; mỗi hội viên chỉ 1 lần/năm) và `GrantAsync` ghi nhận tặng điểm (điểm × `ParamValue` = tiền), cùng `ReconciliationAsync` đối soát điểm/tiền đã tặng theo chương trình + loại thẻ. API `/api/v1/birthday-policies*` + `/api/birthday-policy/check|grant`, controller `BirthdayPolicy` + view Index/Detail/Reconciliation + mục menu, seed 1 chương trình mẫu (GOLD 500 điểm, PLATINUM 1.000 điểm, tỷ lệ 1.000), 13 test mới. Build Release 0 error, 111/111 test pass. Commit `0be240f` đã push origin/main.
- 2026-09-23 — Port nghiệp vụ **chính sách xếp hạng thẻ** (nguồn `Mst_RankPolicy` + logic `Crd_CardRankPolicy_PerformX` / `Mst_RankPolicy_GetUp` trong `CardRank.cs`). Thêm entity `RankPolicy` (mã, hạng thẻ `CardType`, bậc `Value`, ngưỡng điểm/lượt để nâng hạng `PointUpBegin`/`QtyVisitUpBegin` và duy trì hạng `PointKeepBegin`/`QtyVisitKeepBegin`, thời gian duy trì `QtyMonth`, trạng thái Bật/Tạm dừng) + enum `RankPolicyStatus`/`RankActionType`. `RankPolicyService` với vòng đời Bật/Tạm dừng và `EvaluateAsync` (chỉ xét chính sách đang bật; đủ ngưỡng nâng → chọn hạng kế tiếp có `Value` lớn hơn gần nhất; không nâng được nhưng đủ ngưỡng duy trì → giữ hạng; ngược lại giữ nguyên). API `/api/v1/rank-policies*` + `/api/rank-policy/evaluate`, controller `RankPolicy` + view Index/Detail + mục menu, seed 3 hạng mẫu (SILVER/GOLD/PLATINUM), 9 test mới. Build Release 0 error, 133/133 test pass. (Sửa kèm 2 lỗi build/test tồn tại ở HEAD: `params = t.Params.Count` là từ khoá C# gây lỗi biên dịch → đổi thành `paramCount`; `e.Ignore(x => x.Window)` truyền method group không hợp lệ → bỏ.)- 2026-09-23 — Port nghiệp vụ **chính sách quy đổi tiền dịch vụ → điểm** (nguồn `Mst_PolicyMoneyToPointService` + `Mst_PolicyMoneyToPointServiceDtl`, logic tính điểm trong `Card.Deal.cs`). Thêm entity `PolicyMoneyToPoint` (mã, tên, hiệu lực, trạng thái Bật/Tạm dừng) + `PolicyMoneyToPointDtl` (theo hạng thẻ `CardType`: `ConvertValue` giá trị quy đổi, `ConvertPoint` điểm quy đổi, `ValueRankCardType` mốc doanh thu tính lượt xét hạng, `DiscountRate` % chiết khấu dịch vụ) + enum `PolicyMoneyToPointStatus`. `PolicyMoneyToPointService` với vòng đời Bật/Tạm dừng (`SetStatusAsync` chặn bật khi thiếu dòng hạng thẻ), `AddDetailAsync` (chặn trùng hạng thẻ, validate giá trị/điểm/chiết khấu) và `CalcAsync` quy đổi theo công thức nguồn `Point = Amount × (ConvertPoint / ConvertValue)`, tính `qtyVisit = 1` khi doanh thu ≥ `ValueRankCardType`, trả kèm `DiscountRate`; chỉ xét chính sách đang bật + trong khoảng hiệu lực. API `/api/v1/policy-money-to-points*` + `/api/policy-money-to-point/calc`, controller `PolicyMoneyToPoint` + view Index/Detail + mục menu, seed 1 chính sách mẫu (SILVER 1.000đ→1 điểm, GOLD 1.000đ→2 điểm ck 5%, PLATINUM 1.000đ→3 điểm ck 10%), 10 test mới. Build Release 0 error, 143/143 test pass. Commit `abf1994` đã push origin/main.
- 2026-09-23 — Port nghiệp vụ **chiết khấu hội viên** (nguồn `Crd_MemberDiscountTransaction` + logic tính trong `Card.Deal.cs`, `Transaction.cs`). Thêm entity `MemberDiscountTransaction` (số giao dịch `RefNo`, đại lý, hội viên/thẻ, `CardTypeUse`/`CardTypeInit`/`CardTypeApply`, `DealPointType`, `PolicyCode`, `PolicyDiscountRate`, `AmountForDC`, `PointChTotal`) + enum `MemberDiscountPointType`. `MemberDiscountService` với `CalcAsync` (chỉ tính dòng có cờ `FlagDiscount`; giá trị chiết khấu mỗi dòng = `AmountForDC × tỷ lệ chiết khấu đối tượng thanh toán/100 × tỷ lệ chiết khấu hạng thẻ áp dụng/100`, tỷ lệ hạng thẻ lấy từ chính sách quy đổi tiền → điểm đang hiệu lực theo `CardTypeApply`), `RecordAsync` (chỉ ghi nhận khi tổng chiết khấu > 0) và `ReconciliationAsync` (đối soát theo hạng thẻ áp dụng: số giao dịch/tiền được chiết khấu/tổng chiết khấu). API `/api/v1/member-discounts*` + `/api/member-discount/calc|record`, controller `MemberDiscount` + view Index/Detail/Reconciliation + mục menu, seed 2 giao dịch mẫu (GOLD 5%, PLATINUM 10%), 7 test mới. Build Release 0 error, 150/150 test pass. Commit `38943d7` đã push origin/main.- 2026-09-24 — Port nghiệp vụ **danh mục loại khuyến mại** (nguồn `Mst_PromotionMainType` + `Mst_PromotionPrmType` + `Prm_PrmInMain`, hằng số `PRMMainType`/`PromotionPrmType` trong `Const.Main.BE.cs`, dữ liệu khởi tạo `20200416.Init.Data.sql`). Thêm entity `PromotionMainTypeDef` (mã `PRMMainType`, tên, trạng thái — "Khuyến mại theo": Đơn hàng/Hàng hóa/Hàng hóa và đơn hàng), `PromotionPrmTypeDef` (mã `PRMPrmType`, tên, trạng thái — "Hình thức khuyến mại": Giảm giá đơn hàng/Tặng hàng/Giảm giá hàng/Giảm giá bán theo SL mua/Tặng voucher) và `PromotionPrmInMain` (gắn hình thức vào loại khuyến mại theo, cờ bật/tạm dừng). `PromotionTypeService` với CRUD 2 danh mục (chặn trùng mã, tự sinh mã), bật/tạm dừng, `AddMappingAsync` (chặn gắn trùng 1 hình thức vào 1 loại) và `CheckPrmInMainAsync` (chỉ đạt khi loại + hình thức + dòng gắn đều đang bật). API `/api/v1/promotion-main-types*`, `/api/v1/promotion-prm-types*`, `/api/v1/promotion-prm-in-mains*` + `/api/promotion-type/check`, controller `PromotionType` + view Index (3 bảng + form thêm/gắn/kiểm tra) + mục menu, seed 3 loại + 5 hình thức + 7 dòng gắn mẫu, 11 test mới. Build Release 0 error, 161/161 test pass. Commit `d9962f1` đã push origin/main.- 2026-09-24 — Port nghiệp vụ **mã giảm giá + ánh xạ đại lý** (nguồn `Inos_DiscountCode` + `Inos_DiscountCodeTypes` + `Map_DealerDiscount`, logic kiểm tra mã trong `Master.cs`/`OS.Inos.1.cs`). Thêm entity `DiscountCode` (mã, mô tả, kiểu giảm `DiscountType`=Percent/Absolute, giá trị `DiscountAmount`, số lượt còn lại `RemainQty`, trạng thái `Enabled`, khoảng hiệu lực `EffectDateFrom..EffectDateTo`) + enum `DiscountCodeType` và `DealerDiscountMap` (gán đại lý `DealerCode` ↔ mã giảm giá, cờ `FlagActive`). `DiscountCodeService` với CRUD mã (chặn trùng mã, validate giá trị > 0, % ≤ 100, ngày kết thúc ≥ bắt đầu), bật/tắt mã, gán/bật-tắt ánh xạ đại lý (chặn gán trùng), `CheckAsync` (mã phải tồn tại + đang bật + còn lượt + trong hiệu lực — theo nguồn: mã phải tồn tại và `Enabled` mới hợp lệ) và `ApplyAsync` (tính giảm theo %/số tiền, chặn không vượt giá trị đơn, trừ 1 lượt sử dụng). API `/api/v1/discount-codes*` + `/api/v1/dealer-discount-maps*` + `/api/discount-code/check|apply`, controller `DiscountCode` + view Index/Detail + mục menu, seed 2 mã mẫu (SALE10 10%, GIAM50K 50.000đ) + 2 ánh xạ đại lý, 13 test mới. Build Release 0 error, 173/173 test pass. Commit `b4c6a0c` đã push origin/main.- 2026-09-24 — Port nghiệp vụ **sinh mã voucher theo hệ cơ số 36 + checksum** (nguồn `Seq_VoucherID` + `Mst_VoucherID`, logic `Seq_VoucherID_GetByAmount` trong `Seq.cs`, hàm `CMyBase36.To36/To10` trong `Utils.cs`). Thêm entity `VoucherIdSequence` (số thứ tự `Seq` tăng dần, mã `VoucherNo` duy nhất, `VerGen`, thời điểm sinh). `VoucherIdService` với `GenerateAsync` (sinh 1..1000 mã: mã 12 ký tự base36 = `{VerGen 2}{Năm 2}{Tháng 1}{Ngày 1}{NgẫuNhiên 5}{Checksum 1}`, ngẫu nhiên = `Seq % 10.000.000` đổi base36 độ dài 5, checksum = tổng giá trị base36 của 11 ký tự đầu lấy dư 36) và `Validate` (kiểm tra độ dài 12 + ký tự base36 + checksum). API `/api/v1/voucher-id-sequences` + `/api/v1/voucher-id/generate|validate` + minimal-API `/api/voucher-id/generate|validate`, controller `VoucherId` + view Index (bảng mã đã sinh + form sinh/kiểm tra) + mục menu, seed 2 mã mẫu, 8 test mới. Build Release 0 error, 181/181 test pass.- 2026-09-24 — Port nghiệp vụ **tặng điểm giới thiệu** (nguồn `Crd_Member_PerformIntroX` trong `Transaction.AddPoint.cs` + bảng `Crd_CardTransaction` với `DealPointType='INTRODUCTION'`, tỷ lệ quy đổi `Mst_ParamSys.UNITPOINTTOMONEY`). Thêm entity `IntroductionGrant` (số giao dịch `RefNo` dạng `INT.yyyyMMdd.HHmmss`, hội viên ĐƯỢC thưởng `MemberNo` = người giới thiệu, hội viên mới `NewMemberNo`, thẻ/hạng thẻ, đại lý, `PointChTotal`/`AmountChTotal`/`ParamValue`, hạn dùng `PointExpiryDTime` = cuối tháng 12 năm kế tiếp) + enum `IntroductionPointType`. `IntroductionGrantService` với `GrantAsync` (cộng `PointIntro` điểm cho NGƯỜI GIỚI THIỆU chứ không phải hội viên mới; chặn tự giới thiệu; chặn thiếu người giới thiệu/điểm ≤ 0; idempotent mỗi hội viên mới chỉ thưởng 1 lần theo `NewMemberNo`; `paramValue ≤ 0` → mặc định 1; `RefNo` thêm hậu tố khi trùng giây), `GrantsAsync` (lọc theo hội viên) và `ReconciliationAsync` (đối soát số lượt/tổng điểm/tổng tiền theo hội viên được thưởng). API `/api/v1/introduction-grants*` + `/api/introduction/grant`, controller `IntroductionGrant` + view Index/Reconciliation + mục menu, seed 1 nhật ký mẫu (HV001 giới thiệu HV100, 1.000 điểm × 1.000 = 1.000.000đ), 10 test mới. Build Release 0 error, 190/190 test pass.- 2026-09-24 — Port nghiệp vụ **luật giới hạn ưu đãi theo ngày cho chương trình khuyến mại theo loại thẻ** (nguồn `Crd_DealUsePromotion_SaveX` trong `Master.Loyalty.cs`: "Trong 1 ngày + 1 chương trình + 1 Mã hội viên + Mã kỳ thẻ số lượng ghi nhận ưu đãi không được > 1"). Thêm trường `MemberNo` vào entity `CardPromotionUsage` (mã hội viên suy ra từ thẻ) và mở rộng `CardPromotionProgramService.UseAsync` nhận thêm `memberNo` + `at`: khi có hội viên, chặn nếu tổng `QtyUsed` đã ghi nhận trong cùng ngày (theo `UsedAt`) cho cùng chương trình + cùng loại thẻ + cùng hội viên vượt quá 1 (không truyền hội viên → bỏ qua luật ngày, chỉ còn giới hạn tổng `Qty`). API `/api/card-promotion/use` nhận thêm `memberNo`/`at`, action `CardPromotionProgram/Use` + form ghi nhận thêm ô Mã hội viên, 4 test mới (cùng hội viên cùng ngày bị chặn, khác ngày cho phép, khác hội viên cho phép, không hội viên bỏ qua luật). Build Release 0 error, 194/194 test pass. Commit `96b151f` đã push origin/main.- 2026-09-24 — Port nghiệp vụ **phát voucher sinh nhật** (nguồn `Crd_MemberVoucher` + `Crd_MemberVoucherTransaction`, logic `Crd_Member_PerformVCBirhday` trong `Transaction.Birthday.cs`, nâng cấp `20260518.UpgradeBirthdayVoucher.md`). Đây là nghiệp vụ MỚI, tách biệt với điểm sinh nhật đã port trước đó. Thêm entity `BirthdayVoucher` (mã `BV.YYYY.{MemberNo}` idempotent theo năm, `RefNo` audit `VCSN.*`, đại lý phát hành `SUPPORT`, `PointVCTotal`/`PointVCRemain`/`PointVCLimit`, `QtyUseVCLimit`/`QtyUseVCRemain`, `PointExpiryDate`) + enum `BirthdayVoucherPointType`; mở rộng `BirthdayPolicy` thêm cờ `FlagVoucher` và `BirthdayPolicyDtl` thêm `VoucherValue`/`VoucherExpireDays` (nguồn `Mst_BirthPolicy.FlagVoucher` + `Mst_BirthPolicyDtl.VoucherPointVCTotal`/`VoucherExpireDays`). `BirthdayVoucherService` với `CheckEligibilityAsync` (chương trình đang bật + có cờ voucher + trong hiệu lực; ngày sinh trùng tháng-ngày; hạng thẻ có `VoucherValue > 0`; mỗi hội viên 1 voucher/năm) và `IssueAsync` (tạo voucher điểm = `VoucherValue`, hạn dùng = ngày xét + `VoucherExpireDays`, đại lý `SUPPORT`), cùng `ReconciliationAsync` đối soát điểm đã phát/còn lại theo chương trình + hạng thẻ. API `/api/v1/birthday-vouchers*` + `/api/birthday-voucher/check|issue`, controller `BirthdayVoucher` + view Index/Detail/Reconciliation + mục menu, seed voucher sinh nhật mẫu (GOLD 500 điểm, hạn 30 ngày), 8 test mới. Build Release 0 error, 202/202 test pass. Commit `bd85396` đã push origin/main.- 2026-09-24 — Port nghiệp vụ **tra cứu chương trình ưu đãi khả dụng cho một hội viên/thẻ** (nguồn `Crd_Card_GetForPromotion` trong `Master.Loyalty.cs`). Thêm record `AvailablePromotionRow` + `CardPromotionProgramService.AvailableForCardAsync(cardType, dealerCode, at?)`: chỉ xét chương trình khuyến mại theo loại thẻ ĐANG BẬT + trong khoảng `EffDateStart..EffDateEnd`; dòng loại thẻ khớp `CardTypeUse` và `FlagActive` + còn hạn mức (`Qty − tổng QtyUsed > 0`); phạm vi đại lý theo `FlagAllDL` hoặc đại lý của thẻ (`DLCodeRegis`) nằm trong `CardPromotionProgramSpec` — đúng luật 2 nhánh `#tbl_Crd_DealUsePromotionDtl_Filter_AllDL`/`NotAllDL` của nguồn. API `GET /api/v1/card-promotion-programs/available` + minimal-API `GET /api/card-promotion/available` (công khai), controller `AvailablePromotion` + view `Index` (form tra cứu theo loại thẻ/đại lý/ngày + bảng hạn mức/đã dùng/còn lại) + mục menu "Ưu đãi khả dụng cho thẻ", 5 test mới. Build Release 0 error, 207/207 test pass.